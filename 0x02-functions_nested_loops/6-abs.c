#include "main.h"
/**
 * _abs - Computes the absolute value of an interger
 * @k: the interger to be computed
 * Return: The absolute value
 */
int _abs(int k)
{
	if (k >= 0)
	{
		return (k);
}
else
{
	return (-t);
}

}
