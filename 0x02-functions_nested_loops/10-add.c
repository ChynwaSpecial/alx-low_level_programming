#include "main.h"
/**
 * add - Adds two intergers and returns the result
 * @a: the first number
 * @b: the second number
 * Return: the addition of the numbers
 */
int add(int a, int b)
{

	return (a + b);
}
